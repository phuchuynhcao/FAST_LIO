<launch>
  <!-- Launch file for velodyne16 VLP-16 LiDAR -->
    <arg name="project" default="fast_lio"/>
    <arg name="rviz" default="true" />
    <arg name="is_record_odom" default="false"/>
    <arg name="is_record_velodyne" default="false"/>
    <arg name="bags_directory" value="$(find fast_lio)/Bags"/>

    <rosparam command="load" file="$(find fast_lio)/config/velodyne_private_data.yaml" />

    <param name="feature_extract_enable" type="bool" value="0"/>
    <param name="point_filter_num" type="int" value="4"/>
    <param name="max_iteration" type="int" value="3" />
    <param name="filter_size_surf" type="double" value="0.5" />
    <param name="filter_size_map" type="double" value="0.5" />
    <param name="cube_side_length" type="double" value="1000" />
    <param name="runtime_pos_log_enable" type="bool" value="0" />

    <!-- Run sensor driver -->
    <include file="$(find fast_lio)/launch/include/sensors.launch" />

    <!-- Run fast_lio package -->
    <node pkg="fast_lio" type="fastlio_mapping" name="laserMapping" output="screen" /> 

    <group if="$(arg rviz)">
    <node launch-prefix="nice" pkg="rviz" type="rviz" name="rviz" args="-d $(find fast_lio)/rviz_cfg/loam_livox.rviz" />
    </group>

    <!-- Record bag -->
    <node name="bag_record" pkg="rosbag" type="record"
        args="record -o $(arg bags_directory)/bag /Laser_map /Odometry /clicked_point /clock /cloud_effected /cloud_registered /cloud_registered_body /fix_rtk /gnss /imu/data /initialpose /move_base_simple/goal /path /rosout /rosout_agg /tf /tf_static /velodyne_points"
        if="$(eval is_record_velodyne or is_record_odom)"/>

</launch>